/**
 * 对比上一次inputs，没变化就返回上一次配置
 * @param {Array} inputs 
 * @param {Object} lastConfig 
 * @returns Null or Object
 */
export default function diff (inputs, lastConfig) {
  if (!lastConfig) {
    return null
  }

  let diff = false

  return diff ? null : options
}
